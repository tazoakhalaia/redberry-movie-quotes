@props(['class' => '', 'buttonName'])

<div>
    <button class="{{ $class }}
            text-white font-bold text-sm py-2 px-4 border
            rounded uppercase">{{ $buttonName }}</button>
</div>
